root: "ctx_params"
schemas:
  ctx_params:
    type: "dict"
    props:
      hosts_file:
        type: "str"
      hosts_template:
        type: "str"
      hosts_content:
        type: "str"
      meta:
        type: "dict"
